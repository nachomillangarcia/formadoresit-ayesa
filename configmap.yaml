apiVersion: v1
kind: ConfigMap
metadata:
  name: api-java
data:
  LOG_LEVEL: "INFO"
  foo: bar
  myvar: myvalue
  message.txt: |
    <html><body>
      <h1>
        Hello from Kubernetes version 3
      </h1>
      <p>
        Served through configmap and ingress
      </p>
    </body></html>