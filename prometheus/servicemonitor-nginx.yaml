apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-metrics
  namespace: monitoring
  labels:
    app: nginx
spec:
  endpoints:
  - port: prometheus
  jobLabel: app
  selector:
    matchLabels:
      app: nginx
  namespaceSelector:
    matchNames:
    - default