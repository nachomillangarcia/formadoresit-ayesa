apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ Release.Namespace }}
spec:
  selector:
    app: api-java
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 8080